<Window x:Class="Test.newNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My Dialog Box" Width="500" Height="400"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        BorderBrush="#FF673AB7"
        BorderThickness="2">
    <StackPanel Margin="10">
        <TextBlock Text="Jegyzet Címe:" Margin="10"/>
        <TextBox x:Name="textBox1" Height="40" Margin="10" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled"/>
        <TextBlock Text="Jegyzet Tartalma:" Margin="10"/>
        <TextBox x:Name="textBox2" Height="150" Margin="10" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="OK" Click="OK_Click" Margin="10" Width="100"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="10" Width="100"/>
        </StackPanel>
    </StackPanel>
</Window>